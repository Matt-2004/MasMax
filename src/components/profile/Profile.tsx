const Profile = () => {
  /* 
            Features - profile icon change | add image 
                        can change email from database
                                   password | verification
                                   username
      */

  return <div>Profile</div>;
};

export default Profile;
